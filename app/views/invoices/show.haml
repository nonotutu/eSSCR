= render "header", :selected_item => 'show'

Customer_data:
= @invoice.customer_data ? @invoice.customer_data : 'not filled yet'
%br/

Sent at:
= @invoice.sent_at ? @invoice.sent_at : 'not filled yet'
%br/

= link_to "Edit", edit_invoice_path
= link_to "Destroy", { :action => :destroy, :id => @invoice.id }, :method => :delete