
= render 'invoices/list'

.main
  = render 'invoices/header', :selected_item => 'show'

  .section
    = render "data"
    
    %hr.intrasection
  
  .lien_bas_de_page
    ➤
    = link_to "Facture", invoice_path(@invoice, :pdf)
    %font{:style => "font-family: HeydingsIconsRegular"} dD
    |
    = link_to "Edit", edit_invoice_path
    |
    = link_to "Destroy", { :action => :destroy, :id => @invoice.id }, :method => :delete