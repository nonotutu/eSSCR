.menu
  %ul
    %li
      = link_to "☰", invoices_path
      
    %li
      - active = selected_item == 'overview' ? :active : nil
      = link_to "facture ", invoice_overview_path(@invoice.id), :class => active
      
    %li
      - active = selected_item == 'show' ? :active : nil
      = link_to "général", invoice_path(@invoice.id), :class => active
      
    %li
      - active = selected_item == 'events' ? :active : nil
      = link_to "événements", invoice_events_path(@invoice.id), :class => active
      %font.discret= @invoice.events.count
      
    %li
      - active = selected_item == 'nositems' ? :active : nil
      = link_to "facturation", invoice_nositems_path(@invoice.id), :class => active
      %font.discret= @invoice.nositems.count