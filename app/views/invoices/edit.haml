= render 'invoices/list'

.main
  = render 'invoices/header', :selected_item => 'show'

  .section
    = render 'form'

    %hr.intrasection
   
  .lien_bas_de_page
    ➤
    = link_to "Back", invoice_path