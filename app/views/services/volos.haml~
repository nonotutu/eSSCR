= params[:service_id]


= render 'events/list', :service_id => params[:service_id]

.contenu
  = render 'events/header', :selected_item => 'services'
  = render 'header', :selected_item => 'volos'

  = render "list_volos"
  %hr
  - unless params[:mode] == 'with_edit'
    = link_to "Add/Remove volunteers", event_service_volos_path(@event, @service, :mode =>'with_edit')
  - else
    = render "servolos/form"
    = link_to "Back", event_service_volos_path
  %hr
