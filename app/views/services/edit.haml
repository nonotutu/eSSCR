
= render 'events/list'

.main
  = render 'events/header', :selected_item => 'services'
  = render 'header', :selected_item => 'show'

  .section
    = render 'form'
    
    %hr.intrasection_top_links
    
    .liens_intrasection
      = link_to "⇇ back", event_service_path

    %hr.intrasection_bottom_links
