
= render 'events/list'

.main
  = render 'events/header', :selected_item => 'services'
  = render 'services/header', :selected_item => 'servolos'

  .section
    = render "graph_2"
    %hr.intrasection
    = render "table"
    %hr.intrasection
    %ul.empty
      - string_total_servolos(@service.id).each do |ligne|
        %li
          = ligne
    %hr.intrasection


    - if params[:mode] == 'edit'
      = render "form"
    
  .lien_bas_de_page
    - unless params[:mode] == 'edit'
      ➤
      = link_to "Manage", event_service_servolos_path(:mode =>'edit')
      |
      = link_to "Back", event_service_overview_path(@event, @service)
    - else
      ➤
      = link_to "Back", event_service_servolos_path
      - if params[:servolo_id].present?
        |
        = link_to 'Clear (create new)', event_service_servolos_path(:mode => 'edit')