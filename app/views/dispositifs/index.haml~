.main
  = render 'tools/header', :selected_item => 'dispositifs'
  
  .section

    %table.ligne
      %tr
        %th Position
        %th Dénomination
        %th Image 16
      - Dispositif.order(:pos).all.each do |dispo|
        %tr
          %td.mono_droite
            = dispo.pos
          %td
            = dispo.name
          %td
            = image_tag("/dispositifs/code_image_s1/#{dispo.id}", :alt => "☒")
          -#
            %td
              = image_tag("/dispositifs/code_image_s2/#{dispo.id}", :alt => "☒")
            %td
              = image_tag("/dispositifs/code_image_s4/#{dispo.id}", :alt => "☒")
          %td.taille_min
            = link_to 'show', dispositif_path(dispo.id)
    
    .lien_bas_de_page
      ➤
      = link_to 'New', new_dispositif_path